# IDOR Model Evaluation

This document outlines the evaluation of the machine learning model trained to detect IDOR vulnerabilities.

## Model Performance

The model was trained on a very small dataset, which resulted in poor performance. The validation set accuracy was 50%, and the model failed to correctly classify unauthorized requests.

**Note:** The model needs to be retrained on a larger, more representative dataset to be effective.
