# ─────────────────────────────────────────────────────────────────────────────
# WebScan Pro — Cloud Deployment Requirements
# Optimized for Streamlit Community Cloud (1 GB RAM limit)
# PyTorch and TensorFlow excluded — app runs in Rule-Based + sklearn mode
# ─────────────────────────────────────────────────────────────────────────────

# Web & HTTP
streamlit>=1.32.0
requests>=2.31.0
beautifulsoup4>=4.12.0
lxml>=5.1.0

# Data
pandas>=2.1.0
numpy>=1.26.0
joblib>=1.3.0

# ML — scikit-learn only (Random Forest IDOR + SQLi models work fine)
scikit-learn>=1.4.0

# RAG / NLP — lightweight embeddings only
# Note: Full RAG (flan-t5-large) requires HUGGINGFACEHUB_API_TOKEN env var
faiss-cpu>=1.7.4
langchain>=0.1.0
langchain-community>=0.0.20
langchain-huggingface>=0.0.3
langchain-core>=0.1.0
huggingface-hub>=0.20.0

# Reporting
tabulate>=0.9.0

# Utilities
python-dotenv>=1.0.0
